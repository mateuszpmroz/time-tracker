import styled from 'styled-components/native';

export const Container = styled.View({
  margin: 20,
  marginBottom: 30,
});

export const InputContainer = styled.View({
  flex: 1,
});

export const AddTaskInputContainer = styled.View({
  flexDirection: 'row',
  justifyContent: 'space-between',
});
